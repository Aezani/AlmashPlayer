Title: fix: add player improvements and manifest tweaks

- PlayerActivity: use DefaultHttpDataSource.Factory with redirects/timeouts and DefaultMediaSourceFactory; add retry logic and playback state logging.
- AndroidManifest: add FOREGROUND_SERVICE permission and explicit exported attributes for activities.

How to test:
1. Build the app and run on a device/emulator.
2. Play an episode that previously failed; observe UI and logcat (filter: PlayerActivity).
3. If failure repeats, attach logcat output.
